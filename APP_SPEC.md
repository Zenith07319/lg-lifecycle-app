# ROR 앱 구성 명세 (구현 기준)

> **목적**: 프론트엔드/백엔드가 동일한 화면·컴포넌트·데이터·동작으로 구현하기 위한 앱 설계 명세.
> **기준 목업**: 루트 `ROR_앱목업.html` (동작하는 클릭 데모 — 계산 엔진 포함).
> **짝 문서**: `app/backend/API_CONTRACT.md`(요청/응답 계약), `project/docs/04_calculation_logic.md`(계산 로직 정본).
> 작성 2026-06-04. 플랫폼: **ThinQ 내 모바일 앱** (프론트 Next.js / 백엔드 FastAPI).

## 0. 절대 원칙 (UI/문구에 반드시 반영)
- 구독·교체를 **무조건 추천 금지** — 계속사용·수리도 1순위가 될 수 있음. 모든 결과는 "비교 결과" 톤.
- "정확한 고장 예측" 표현 금지 → **"점검 필요도(추정)"**. 모든 결과 화면에 면책 문구.
- 추천은 **가중치 기반 점수화** (LLM 예측 아님).
- 모든 수치는 "현재 입력 조건 기준 추정".

---

## 1. 정보구조(IA) — 하단 탭 + 화면

```
하단 탭: 🏠 홈 | 📱 내 가전 | 🔍 진단 | 🔔 알림 | ⋯ 더보기

홈(s-home)
내 가전(s-devices) ─ 가전 등록(s-register: OCR 기본 + 수기 보조)
진단(s-diagnose, 챗봇 스텝) ─ 결과(s-result)
                                  ├ 추천 제품(s-products: 구매/구독 탭, LG 연계)
                                  ├ A/S Fast Pass(s-fastpass)
                                  └ 서비스센터(s-center)
알림(s-alerts)
더보기(s-more: 진단 이력·구독/추천·가족공유)
( 관리자 콘솔 — FR 범위 외 별도: 기준 데이터 관리 )
```

**내비게이션 규칙**: 하위 화면(등록·결과·추천·FastPass·센터)은 진입한 탭 컨텍스트를 유지(예: 결과 계열은 '진단' 탭 활성). 뒤로가기 제공.

---

## 2. 화면별 명세

각 화면: **목적 / 구성 요소 / 입력·출력 / 동작 / 대응 명세(ROR·FR)**.

### S1. 홈 (`s-home`)
- **목적**: 진입 허브 — 내 가전 상태 한눈 + 진단 시작.
- **구성**: 그라데이션 히어로 / **플로팅 내 에어컨 카드**(모델·용량·월간소비전력량·**건강등급 상태 pill**) / 큰 "지금 진단하기" CTA / 4칸 퀵액션(내 가전·등록·알림·더보기) / 최근 알림 2건.
- **동작**: 건강 pill은 마지막 진단 결과 반영(없으면 '진단 전' 회색). 진단 완료 시 갱신.

### S2. 내 가전 (`s-devices`) / 등록 (`s-register`)
- **목적**: 보유 가전 목록·관리 + 신규 등록.
- **목록 구성**: 가전별 [모델 · 용량 · **월간소비전력량(kWh/월)** · 효율등급 · 연식 · 등록출처(OCR/수기)] + 알림 ON/OFF 토글.
- **등록 흐름(OCR 기본 + 수기 보조)** — ROR-01, FR-AD-100~103, FR-UU-100:
  - 기본: **📷 라벨 촬영(OCR)** → 인식중 → 인식 결과 폼(모델·연식·용량·효율등급·**월간소비전력량**·계약) 확인/수정 → 저장.
  - 보조(촬영 버튼 하단): **✍️ 직접 입력(수기)** — OCR 불가 사용자용. 동일 폼을 직접 입력 모드로(카메라 숨김). 용량 선택 시 효율·월간소비전력량 **제품 DB 자동 보완**.
  - 저장 시 `registration_source = OCR | 수기(MANUAL)`.
- **NFR-08**: OCR 원본 이미지는 인식 후 즉시 폐기(저장 X).

### S3. 챗봇 진단 (`s-diagnose`) — ROR-02, FR-AD-200~206, FR-UU-200
- **목적**: 분기형 설문(챗봇 UI)으로 진단 입력 수집.
- **스텝(6)**: ① 가전 선택 → ② **증상 복수 선택**(성능저하·소음·냄새·누수·전기요금증가·작동불량·증상없음) → ③ **증상별 심각도 1~5점** → ④ 사용환경(하루 사용시간·연간 사용개월) *※평수 없음* → ⑤ 여름 한 달 사용량(kWh)·필터 미청소 개월·수리 이력 → ⑥ **우선순위 3축**(비용/환경/편의, 각 0~100).
- **출력**: 진단 입력 구조화 → 진단 실행 → 결과 화면.
- **규칙**: 필수 미입력 시 다음 비활성. 챗봇=설문이며 생성형 AI 아님.

### S4. 결과 대시보드 (`s-result`) — ROR-03/04/05, FR-AD-304~309, FR-UU-400~403
- **건강등급 카드**: A~E(색상)·건강점수·등급 설명·**점검 필요도 게이지**.
- **전기요금 카드**: 구형/신형 월 기여요금·여름 월·**5년 절감**·계산 기준 노출(`표준 월간 N kWh × 실사용/7.8h ÷ 잔존효율`).
- **5선택지 비교**: 계속사용/셀프케어/수리후사용/구독전환/신제품구매 — 최종점수 막대·5년 총비용·초기비용·경고(하드룰). **1·2·3순위** 정렬.
- **추천 근거 카드**: 1순위 + 우선순위(3축) 반영 설명 + 지표 배지. 가중치 기반 명시.
- **다음 액션**: (수리 상위) A/S Fast Pass / 서비스센터 / (구매·구독 상위) 추천 제품.
- 면책 문구 고정.

### S5. 추천 제품 (`s-products`) — ROR-10/11, FR-AD-505/506 · **LG 연계**
- **노출 조건**: 신제품구매/구독전환이 **상위권(1~2순위)일 때만** 결과에서 진입(구독·교체 강요 금지 원칙).
- **탭**: 🛒 신제품 구매 / 🔁 구독.
- **카드**: 모델·용량·등급·연 kWh / 예상가(or 월구독료) / **5년 전기료 절감액(엔진 계산)** / 태그(최고효율·가성비·관리포함) / **[LG에서 보기 ›] 딥링크**.
- **정렬**: 우선순위 3축 반영(비용→가격/가성비, 환경→최고효율, 편의→구독).
- **LG 연계(MVP)**: `recommended_product.purchase_url` = LG.com 딥링크/검색URL + `utm_source=ROR` 전환추적. (실시간 가격 API는 확장)
- 면책: "참고가, 실제는 LG.com 확인".

### S5b. A/S Fast Pass (`s-fastpass`) — ROR-08/12, FR-AD-502/503/508
- 개인정보 포함 **동의 토글**(ThinQ 참조) → **PDF 리포트**(제품·연식·건강등급·점검필요도·증상·권장 선택지) 생성/저장/공유(링크 만료·삭제). **수정 불가**.

### S5c. 서비스센터 (`s-center`) — ROR-09, FR-AD-504
- 위치 기반 지도 + 센터 리스트(가까운 순·수리 가능 품목·연락처·예약).

### S6. 알림 (`s-alerts`) — ROR-13, FR-AD-600~603, FR-UU-600
- **알림함**: 이상 사용량·전기요금 급증·**부품보유기간 초과**·점검 주기. 클릭 → 진단 이동.
- **설정**: 가전별 ON/OFF(기본 ON). "이상사용량·요금급증" / "부품보유기간 초과·점검 주기".
- ※ "연식 초과" 알림 기준 = **부품보유기간(에어컨 8년)** 초과(수리 시 부품 수급 신호).

### S7. 더보기 (`s-more`) — ROR-05/10/11, FR-AD-401, FR-UU-306
- 진단 이력(저장·조회) / 구독·추천 제품(→ s-products) / **가족 공유 요약**(비기술 한 단락).

### (관리자) — ROR-14, FR-AD-700/701 (FR 범위 외 별도 콘솔)
- 기준 데이터(제품 스펙·전기요금·비용·탄소·VOC·`purchase_url`) 등록·수정·유효성 검증.

---

## 3. 공통 컴포넌트 (재사용)
| 컴포넌트 | 용도 | 주요 props/state |
|----------|------|------------------|
| HealthGradeCard | 건강등급 A~E + 점검필요도 | grade, score, inspection |
| OptionCompareList | 5선택지 카드/순위/막대 | options[], rank, hl |
| ChatBubbleStep | 챗봇 분기 문답 | step, question, inputType |
| SymptomChips + SeverityStepper | 증상 복수 + 1~5 | selected{symptom:severity} |
| PriorityAxes | 3축 슬라이더 | cost/env/conv (0~100) |
| OcrCamera + RecogForm | 라벨 촬영·인식결과·수기 | regMode(OCR/MANUAL) |
| ProductCard + LgLink | 추천 제품 + LG 딥링크 | model, price, saving, url |
| AlertCard + Toggle | 선제 알림·ON/OFF | type, on |
| DisclaimerBanner | 면책 고지 | (고정) |

---

## 4. 데이터 모델

### 4-1. 프론트 진단 상태 (입력) → API 요청과 매핑
```
device: { model, year, capacity_kw, grade, contract_type, registration_source }
symptoms: [ { type, severity(1~5) }, ... ]   // 복수
daily_usage_hours, usage_months
summer_monthly_kwh, filter_clean_months, repair_history_count
priority: { cost, env, convenience }          // 3축 0~100
```
→ `POST /api/diagnose` (스키마·필드 = `app/backend/API_CONTRACT.md`).

### 4-2. 결과(응답) → 화면
- 요약(건강등급·점수·전기요금 델타) + 전체(diagnosis·ranked_options[5]·carbon_summary·report). → API_CONTRACT §1-4/§2.

### 4-3. 보조 데이터
- **추천 제품 카탈로그** = ERD `recommended_product`(rec_type PRODUCT/SUBSCRIPTION, device_spec_id, estimated_saving, purchase_url). MVP는 관리자 갱신 CSV/DB.
- **알림 로그** = ERD `notification_log`.
- **디바이스 스펙** = `device_energy_spec.csv`(용량 키 → **monthly_kwh**·등급·연kWh).

---

## 5. 계산 로직 (백엔드 — 화면은 표시만)
> 정본: `project/docs/04_calculation_logic.md`. 핵심만:
- **점검 필요도** = 연식0.30 + 증상0.30 + 수리0.15 + 관리0.15 + VOC0.10 (고장확률 아님).
- **건강등급 A~E** = 100 − (점검×0.45 + 에너지낭비×0.30 + 생활불편×0.25)×100.
- **전기요금** = 베이스 **표준 월간 소비전력량** × (실사용시간/**7.8h**) ÷ 잔존효율 → 누진 델타 `Bill(기준+AC)−Bill(기준)`, **부가세10%·전력기금3.7%** 포함.
- **5선택지 5년 비용** + 재수리 기대비용. **3축 가중치**(기본50%+우선50%, 합1.0).
- **기대수명 16.7년**(KESIS HEPS 역산) · **부품보유기간 8년**(초과 시 수리 패널티·알림).

---

## 6. 확정 사항 / 미확정
**확정**: 3축 우선순위 / 증상 복수+심각도 1~5 / **평수 미사용(제거)** / 월간소비전력량 베이스·7.8h / 기대수명 16.7 / 부품보유기간 기준 알림 / 가중치 기반(LLM 아님) / OCR 기본+수기 보조 / LG 딥링크 연계.
**미확정**: 증상 심각도 **5점 배율**(잠정 1.8) — 회의 확정 대기.

---

## 7. 목업 → 실제 구현 매핑 (참고)
| 목업(HTML) | 실제 구현 |
|------------|-----------|
| `runDiagnosis()` JS 엔진 | **백엔드 `POST /api/diagnose`** 호출로 대체(프론트는 결과 렌더만) |
| `SPEC/COST/TARIFF` 상수 | 백엔드 CSV(`device_energy_spec`·`cost_reference`·`electricity_tariff`) |
| 화면 `screen` 전환 | Next.js 라우트/페이지(또는 탭 컴포넌트) |
| `PRODUCTS/SUBS` 카탈로그 | `recommended_product`(DB) + LG URL |
| `openProducts`·`go()` | 라우팅 |

> 목업은 프론트가 백엔드 없이도 화면·동작을 검증한 레퍼런스. 실제 계산은 **백엔드 단일 엔진**(`app/backend`)이 담당하고, 프론트는 API 결과를 표시한다.
